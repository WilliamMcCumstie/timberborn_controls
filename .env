# SCRAP_METALS_MODE controls what scrap_metals depends on.
#
# Valid values:
#   none     — scrap_metals has no dependencies (default)
#   basic    — scrap_metals depends on treated_planks
#   enhanced — scrap_metals depends on treated_planks and extracts
#
SCRAP_METALS_MODE=none
